  <div class="col-xs-6 col-sm-6 col-sm-offset-1">
    <div class="card" style="background-image:linear-gradient(-225deg, rgba(0,0,0,0.3) 0%, rgba(0,0,0,0.5) 50%),url('<%= cl_image_path @instrument.photo if @instrument.photo.present? %>'); background-size: 250px; opacity:0.9;margin-top: 25vh;">
      <div class="card-description">
      <!-- insert <div class="card"> -->
        <h1><%= @instrument.title %></h1>
        <p><%= @instrument.description %></p>
        <p><%= @instrument.marque %></p>
      </div>
    </div>
  </div>
<%= simple_form_for([@instrument, @location]) do |l| %>
  <div class='col-sm-4 '>
    <div class="form-group">
      <%= l.input :start_date, as: :string, required: false, input_html: {class: "datepicker"} %>
      <%= l.input :end_date, as: :string, required: false, input_html: {class: "datepicker"} %>
    </div>
    <div class="action">
      <%= l.submit  "book ", class: "btn btn-primary " %>
    </div>
  </div>
<% end %>









